# 开发文档

## 2026-04-21 项目文档体系初始化

### 当前状态

- 已完成项目级文档框架搭建。
- 已明确本项目分为两个阶段：
  - 阶段 A：现有小智功能移植到当前硬件。
  - 阶段 B：交互体验优化。
- 已完成对现有仓库架构的初步梳理，明确后续应通过新增目标板目录完成适配。

### 已完成内容

- 新增项目文档导航。
- 新增需求文档，沉淀项目背景、目标、范围、约束、验收标准。
- 新增方案文档，沉淀分层架构、板级适配策略、关系图、时序图、实施顺序和测试建议。
- 新增硬件映射与参考板分析文档，完成第一版原理图结构化整理。
- 新增独立板型 `camera-xiaozhi` 骨架，并接入 `Kconfig` / `CMake` / `config.json` / `README`。
- 明确了当前从原理图能够确认的硬件基础信息：
  - 主控为 `ESP32-S3`
  - 板载 `8MB PSRAM + 16MB Flash`
  - 存在显示、音频、RGB 灯、IO 扩展、DVP 摄像头、电池充电与电源检测
  - 参考基线优先选择 `zhengchen-cam`
  - 根据新增资料，软件落地基线调整为 `lichuang-dev`
  - 屏驱动已确认为 `ST7789`
  - 摄像头模组已确认为 `GC0308`

### 暂未开始内容

- 引脚与器件映射表整理。
- 板级驱动实现。
- 编译配置验证。
- 实机联调与功能回归。

### 未完成项

- 补充结构化硬件资料：
  - 器件型号清单
  - 电源和下载方式说明
- 在文档基础上进入编码实现与测试阶段。
- 校验 `camera-xiaozhi` 与实物是否确实可完全沿用 `lichuang-dev` 摄像头引脚。
- 根据实机结果决定是否补充电池/充电状态检测。

### 风险记录

- 当前原理图文本无法完整提取所有外设连接关系，直接编码有较高返工风险。
- 当前页 1 与页 2 对 `GPIO3` 的用途存在冲突，需通过实物或新版资料确认。
- 当前 `camera-xiaozhi` 的摄像头引脚按 `lichuang-dev` 基线实现，这是用户确认后的工程假设，仍需实机验证。
- 若后续目标板名称、分区策略或网络形态发生变化，需要同步修订方案文档。
